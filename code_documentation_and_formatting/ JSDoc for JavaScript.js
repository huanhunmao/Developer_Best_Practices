// ✅ Good JSDoc Example 
// 如果要写 函数注释 就要把函数 作用/参数/返回 都写出来 
/**
 * Adds two numbers together.
 * @param {number} a - The first number.
 * @param {number} b - The second number.
 * @returns {number} The sum of the two numbers.
 */
function sum(a, b) {
    return a + b;
  }


// bad ❌  写半截 不如不写
// Adds a and b
function sum(a, b) {
  return a + b;
}
// Missing detailed JSDoc comment



